# ffmpeg could'nt be installed...
# sudo apt update
# sudo add-apt-repository universe
# sudo apt dist-upgrade

# so using sox:
sudo apt-get install sox
sudo apt-get install lame
sudo apt-get install id3v2